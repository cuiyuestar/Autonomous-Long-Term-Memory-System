/**
 * Top-level package marker scanned by the Harness Client module registry.
 *
 * @module @altm/deepseek-harness
 */

/** The package root has no Host behavior; roles live on explicit subpaths. */
export function apply(): void {}

export default { apply };
